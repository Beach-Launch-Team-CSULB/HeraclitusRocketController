/*#include "EventManager.h"

EventManager::EventManager()
{}

int EventManager::getSize() const{
    return this->taskQueue.size();
}

Event EventManager::getFront() const{
    return this->taskQueue.front();
} 

void EventManager::pushEvent(Event event) {
    this->taskQueue.push(event);
}

void EventManager::popEvent() {
    this->taskQueue.pop();
}

*/